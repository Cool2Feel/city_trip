# TREK 对标分析与小程序优化可行性报告

> 分析对象：参考项目 [liketrek/TREK](https://github.com/liketrek/TREK) × 当前「周末城市游」微信小程序
> 分析日期：2026-08-10
> 目的：解析 TREK 的产品/技术设计，盘点当前小程序能力，评估**借鉴 TREK 优化与改进本小程序**的可行性，并给出落地优先级。

---

## 〇、执行摘要（结论先行）

**TREK 是什么**：一个自托管、支持实时多人协作的全生命周期旅行规划工具（规划→预订→打包→开销→日志→统计），技术栈为 Node.js/NestJS + React/SQLite，地图用 Leaflet/Mapbox GL，并通过 MCP 把能力开放给 AI。

**当前小程序是什么**：一个轻量的「中国城市周末深度攻略」微信小程序，核心价值是**一键生成结构化周末出游建议**（11 个调研方向 + 10 节报告 + 3 条主题路线 + 分类地图）。代码已迭代到 v6/v7（比 v3.0 文档更成熟），UI/交互完善，但**数据本质是 Mock**——19~20 座城市里只有广州/深圳/成都 3 座有真实编排数据，其余均为模板随机生成的占位内容；后端 Express 只是 mockData 的封装壳。

**关键判断**：
- TREK 与小程序**产品形态差异大**（多日跨国行程管理 vs 单城周末灵感发现），不能照搬。
- 但 TREK 在**地图可视化、天气、打包清单、费用、个人行程笔记、足迹统计、离线、分享导出、AI 助手**等模块有大量「可降维移植」的能力。
- **最该借鉴的不是协作/账号/MCP 这些重架构，而是"让攻略从只读变成可用、可规划、可个性化"的轻量功能**。
- 当前小程序**最大瓶颈是数据真实性**，而非功能缺失。TREK 提供的是"功能想象空间"，真实数据接入才是前提。

**Top 5 推荐落地项**（按性价比）：
1. 出行行李/必备清单（借鉴 TREK Packing Lists，纯前端，高价值）
2. 路线地图折线可视化（借鉴 TREK Route Viz，用微信 `<map>` polyline，高价值）
3. 个人行程备注/打卡（借鉴 TREK Trip Notes，本地存储，高价值）
4. 预算估算（借鉴 TREK Costs，基于现有票价/餐饮数据聚合，中价值）
5. 我的足迹/城市打卡统计（借鉴 TREK Atlas，本地计数，中价值）

---

## 一、参考项目 TREK 解析

### 1.1 产品定位
| 维度 | 内容 |
|------|------|
| 标语 | Your trips. Your plan. Your server.（自托管、可协作、含地图/预算/打包/日志/AI） |
| 形态 | Web 应用 + 可安装 PWA（iOS/Android 浏览器添加，无需应用商店） |
| 部署 | Docker 镜像、Helm/K8s，AGPL v3 许可证 |
| 核心场景 | 多日/跨国旅行的「规划—预订—打包—开销—日志—统计」全生命周期管理 |

### 1.2 技术架构
- **后端**：Node.js 22 / NestJS 11 / SQLite / WebSocket(`ws`)
- **前端**：React 19 / Vite / TypeScript / Tailwind / Zustand
- **地图**：Leaflet 与 Mapbox GL（含 3D 建筑、地形、照片标记、聚合、路线可视化）
- **认证**：JWT + OAuth 2.1 + OIDC(SSO) + Passkeys(WebAuthn) + TOTP MFA
- **天气**：Open-Meteo（无密钥，16 天预报）
- **PWA**：Service Worker + Workbox 离线缓存瓦片/API
- **AI**：内置 MCP 服务器（OAuth 2.1，150+ 工具，30 资源）

### 1.3 核心功能模块（与本项目相关的已标注 ★）
- ★ **地图与地点**：交互式地图、地点搜索（Google/OSM 双源）、导入（GPX/KML/KMZ/GeoJSON）、路线优化并回导 Google Maps、地点照片标记、聚合。
- ★ **天气集成**：Open-Meteo 无密钥接入，16 天预报 + 历史气候。
- ★ **打包清单（Packing Lists）**：分类、模板、成员分配、进度跟踪；可选行李重量追踪。
- ★ **费用（Costs）**：Splitwise 风格的分摊、按人/按日拆分、结算、多币种。
- ★ **旅行日志/笔记（Trip Notes / Journey）**：当日时间戳笔记、杂志风日志、照片可接 Immich/Synology。
- ★ **统计（Atlas）**：已访问国家世界地图、愿望单、连续行程 streak、液态玻璃 UI。
- ★ **离线 / PWA**：Service Worker 缓存，离线可用。
- ★ **导出（PDF）**：含封面、图片、笔记的完整行程。
- ★ **AI / MCP**：AI 自动建行程、排日、清单、预算。
- 协作（Collaboration）：WebSocket 实时同步、角色邀请、群聊投票、SSO/2FA/Passkeys——**本项目暂不适配，见第四章 C 级**。
- 预订管理（Booking）：航班/住宿/餐厅，从邮件/PDF 导入——**跨国场景，与周末单城游不匹配**。

---

## 二、当前小程序功能盘点

### 2.1 产品与架构现状
- **页面（9 个）**：home / discover / map / profile / city-select / report / category / route / search；底部 tabBar 4 个（首页/发现/地图/我的）。
- **数据层**：`api.js` 已实现完整的 `cloud → api → cache → mock` 降级链 + SWR + LRU 缓存 + 重试 + 请求去重，但默认 `useRest=true` 指向 `localhost:3001`，而生产实为 Mock。
- **后端**：`server/index.js` 是 Express 壳，**仅包装 mockData**，未接任何外部真实数据源；天气、报告、地点全是 Mock/模板生成。
- **数据真实性（核心短板）**：
  - 19~20 座城市，仅 **广州/深圳/成都 3 座有真实编排报告**；
  - 其余城市由 `generateReport()` 用关键词模板 + `Math.random()` 生成**占位数据**（票价、评分、地铁站名都是假的），且 `generatedAt` 写死 `2026-07-30`；
  - 「周末日期」虽随当前周动态刷新，但数据内容静止 → 跨周后会长期显示"已过期"提示。

### 2.2 已实现功能清单
| 模块 | 能力 |
|------|------|
| 攻略 | 11 调查方向、10 节标准报告、质量检查 5 维评分、工作流动画 |
| 路线 | A/B/C 三条主题路线（文艺/情侣/亲子），含时间表、交通估算、日历导入、地图导航 |
| 地图 | 11 类标记点、分类筛选、双向联动、聚类辅助 `clusterHelper.js`、空状态/骨架屏 |
| 分类 | 按 11 方向查看全清单 |
| 发现 | 分类精选内容流 + 主题卡片（v6.0） |
| 搜索 | 跨城市内容搜索 + 历史 + 热词 |
| 我的 | 微信登录、收藏城市、保存攻略、清除缓存、关于 |
| 交互 | 骨架屏、空状态、触感反馈、快速跳转、全文复制、外链（大麦/点评/高德）、分享 |

### 2.3 当前核心短板
1. **数据假**：17 座城市报告是随机占位，用户一旦切到非广深蓉城市即"穿帮"。
2. **攻略只读**：用户只能看，不能标记"我想去/已去过/注意事项"。
3. **路线无空间感**：A/B/C 路线只有文字时间表，地图上画不出连续路径。
4. **无预算概念**：票价、餐费散落在各处，没有"这条线总共花多少"的聚合。
5. **无天气联动**：天气是季节假数据，未与路线/穿衣建议结合。
6. **无个性化/沉淀**：看过哪些城、去过哪些点，没有"足迹"沉淀。

---

## 三、对标矩阵（TREK 能力 × 小程序现状）

| TREK 能力 | 小程序现状 | 差距 |
|-----------|-----------|------|
| 交互式地图（3D/地形/照片标记） | 微信原生 `<map>` 标记 + 聚类，无 3D/照片 | 中（受平台限制） |
| 路线可视化 | 仅文字时间表，无折线 | 大 |
| 地点导入(GPX/KML) | 无 | 不适用（周末场景） |
| 天气(Open-Meteo) | 季节 Mock | 大（可接真实） |
| 打包清单 | 无 | 大（高价值缺口） |
| 费用分摊 | 无（无预算聚合） | 大（可简化） |
| 当日笔记/日志 | 无个人备注 | 大（高价值缺口） |
| 访问统计(Atlas) | 无足迹 | 中（可本地做） |
| 离线/PWA | api.js 已有 SWR+缓存 | 小（已具备） |
| PDF 导出 | 仅全文复制 + wxacode 海报 | 中（小程序限制） |
| AI/MCP 助手 | 无（数据生成期才有 AI） | 大（需后端 LLM） |
| 实时协作/SSO/2FA | 无账号体系深度 | 不适用（过重） |
| 多语言 | 中文 | 不需要 |

---

## 四、可借鉴功能的可行性评估（核心）

分级标准：**A=高可行性+高价值（强烈建议）｜B=中可行性+中价值（可选）｜C=低可行性/不适配（不建议近期）**。
可行性同时考虑：微信小程序平台限制、与现有 v6/v7 架构的契合度、工作量。

### A 级 — 高可行性高价值（建议优先做，多为纯前端）

#### A1. 出行行李/必备清单（借鉴 TREK Packing Lists）
- **借鉴点**：按类别（证件/数码/洗漱/衣物/防晒…）的清单 + 勾选进度。
- **适配改造**：新增「出行清单」页或嵌入路线页底部；为周末城市游定制模板（如"晴天防晒/雨天折叠伞/充电宝/地铁卡"）。
- **可行性**：⭐⭐⭐⭐⭐ 纯本地 `wx.setStorageSync`，无需后端，复用现有 UI 组件。
- **工作量**：小（1~2 天）。

#### A2. 路线地图折线可视化（借鉴 TREK Route Visualization）
- **借鉴点**：把当日行程在地图上连成一条线并标点。
- **适配改造**：路线页「查看地图」时用微信 `<map>` 的 `polyline` + `includePoints`，按时间线顺序连接各 POI；复用 `clusterHelper` 已有坐标。
- **可行性**：⭐⭐⭐⭐⭐ 微信 `<map>` 原生支持 polyline/markers，坐标已存在。
- **工作量**：小（1 天）。

#### A3. 个人行程备注/打卡（借鉴 TREK Trip Notes）
- **借鉴点**：当天时间戳笔记、图标标签、拖放排序。
- **适配改造**：在 report/route/map 的地点卡片加「记一笔/标记想去/已去过」；本地存储 `notes` 与 `visited` 标记，攻略页顶部显示"我的标记"。
- **可行性**：⭐⭐⭐⭐⭐ 本地存储即可，与现有"收藏城市/保存攻略"体系一致。
- **工作量**：小~中（2~3 天）。

#### A4. 预算估算聚合（借鉴 TREK Costs，做减法）
- **借鉴点**：按行程拆分费用、结算。
- **适配改造**：不需要多人分摊；只做**单条路线的费用预估**——把路线时间线里每个地点的票价/餐费（已有 `price` 字段）聚合为"门票约 X + 餐饮约 Y + 总计 Z"，并区分必花/可选。
- **可行性**：⭐⭐⭐⭐ 数据已在手（票价/餐饮），纯前端计算。
- **工作量**：小（1~2 天）。

#### A5. 我的足迹/城市打卡统计（借鉴 TREK Atlas，做减法）
- **借鉴点**：访问统计、连续记录 streak。
- **适配改造**：本地记录"浏览过/收藏过/标记去过"的城市与 POI 数，我的页展示"已探索 N 城 / 已打卡 M 个点 / 连续周末探索 streak"。
- **可行性**：⭐⭐⭐⭐ 基于现有 `favoriteCities`/`savedReports` 扩展，本地计数。
- **工作量**：小（1~2 天）。

#### A6. 真实天气接入（借鉴 TREK Open-Meteo）
- **借鉴点**：无密钥天气 API，与行程联动。
- **适配改造**：后端 `/v1/weather` 改接 Open-Meteo（或和风/彩云等国内可达源），前端 `getWeather` 已留好接口；可进一步做"雨天路线提醒/穿衣建议"。
- **可行性**：⭐⭐⭐ 技术上简单，但**需在小程序后台配置 request 合法域名**（部署约束）。
- **工作量**：小（后端 0.5 天 + 域名配置）。

### B 级 — 中可行性中价值（可选，部分需后端）

#### B1. 路线自动优化（借鉴 TREK Route Optimization）
- **借鉴点**：按地理邻近/时间窗自动重排地点。
- **适配改造**：在路线页提供"按距离智能重排"按钮，用 `uiHelper.estimateDistance`（已实现的 Haversine）做最近邻排序，减少跨城奔波。
- **可行性**：⭐⭐⭐ 算法纯前端；但只有 3 城有真实坐标，其余城市坐标为随机散布，优化无意义 → **依赖 A6 级的数据真实化**。
- **工作量**：中（2 天，且需真实坐标）。

#### B2. 攻略分享长图/海报（借鉴 TREK PDF Export）
- **借鉴点**：把行程导出为可分享文件。
- **适配改造**：已有 `getWxacode`（小程序码），可新增"生成攻略卡片长图"用 `canvas` 绘制一图速览+路线，分享到朋友圈/群。
- **可行性**：⭐⭐⭐ 小程序 canvas 绘图可行；PDF 在微信内不便，长图更合适。
- **工作量**：中（2~3 天）。

#### B3. 轻量多端同步（借鉴 TREK 协作，做极简版）
- **借鉴点**：多人共享一份行程。
- **适配改造**：通过微信"生成攻略小程序码/分享卡片"实现**异步共享**（非实时协作）；需要实时同编则必须上云开发 WebSocket，性价比低。
- **可行性**：⭐⭐ 异步分享已具备（onShareAppMessage）；实时协作不建议。
- **工作量**：分享能力现成，实时协作高。

#### B4. AI 攻略问答助手（借鉴 TREK AI/MCP）
- **借鉴点**：AI 直接读资源、自动建行程。
- **适配改造**：在发现/路线页加"问一下 AI"入口，云函数调用 LLM（如混元/通义）基于当前城市报告作答（"这周末带老人怎么安排？"）。
- **可行性**：⭐⭐ 需后端 LLM + 费用/合规；属智能化增强，非当务之急。
- **工作量**：中~大（后端+提示词工程 1 周）。

### C 级 — 低可行性 / 不适配（不建议近期做）

| 项 | 原因 |
|----|------|
| 实时多人协作(WebSocket) | 需自建 WS/云开发 + 复杂权限，与"周末灵感"轻量定位不符 |
| SSO / Passkey / 2FA | 微信小程序已有微信身份体系，自建 Passkey 无意义 |
| 预订导入(邮件/PDF 解析) | 跨国航班/酒店场景，与单城周末游不匹配 |
| 3D 地图 / Mapbox GL | 微信 `<map>` 不支持 WebGL/Mapbox GL，平台硬限制 |
| 插件 SDK 架构 | 小程序体量不需要插件化 |
| 多语言(20 国) | 目标用户为中文，无需求 |
| GPX/KML 导入 | 周末步行路线无需专业轨迹导入 |

---

## 五、落地路线图（按阶段与优先级）

### 阶段 1 — 快速见效（纯前端/本地，1~2 周，无需后端改造）
> 直接把 TREK 的"个人可用"能力本地化，零部署风险。
- [ ] A1 出行行李清单
- [ ] A2 路线地图折线（polyline）
- [ ] A3 个人行程备注/打卡（本地）
- [ ] A4 路线预算聚合
- [ ] A5 我的足迹统计
- **产出**：攻略从"只读"变"可规划/可记录/可算钱"。

### 阶段 2 — 数据真实化（需后端/接口，2~4 周）
> 解决核心短板，让 A 级功能在 17 座占位城市也成立。
- [ ] A6 真实天气（Open-Meteo / 国内源）+ request 域名配置
- [ ] 用原 `weekend-city-trip` Skill 的 anysearch+高德管线，**批量生成 17 城真实报告**替换 `generateReport()` 占位逻辑
- [ ] B1 路线自动优化（依赖真实坐标）
- **产出**：数据可信，跨城不生硬。

### 阶段 3 — 智能化（需 LLM/云，可选）
- [ ] B4 AI 攻略助手（云函数 + LLM）
- [ ] 个性化推荐（基于 A5 足迹/偏好）
- [ ] B2 分享长图

### 阶段 4 — 不建议近期
- 实时协作、账号体系增强、插件化（见 C 级）。

---

## 六、关键风险与约束

1. **数据真实性是天花板**：当前 17 城假数据若不解决，任何"路线优化/预算/足迹"在那些城市都会失真。阶段 2 是前提。
2. **微信小程序平台限制**：
   - `request` 必须配置**合法域名白名单**才能调用真实天气/数据接口（本地调试可关校验，但上线必须配）；
   - 主包 ≤ 2MB（当前 0.33MB，空间充裕，但图片/字体要节制）；
   - 不支持 WebGL → 3D 地图、Mapbox GL **不可行**；
   - 不能直接 `window.open` 外链 → 现有"复制+弹窗"降级方案合理，沿用。
3. **合规**：`app.json` 已声明 `getLocation`/`chooseLocation` 权限，上线需在审核说明用途；若地图用静态坐标即可，可考虑移除定位权限以降低审核风险（见发布指南 Q&A）。
4. **与现有迭代一致**：代码已到 v6/v7，本报告 A 级功能应**复用 `categoryHelper`/`uiHelper`/`api.js` 现有模式**（如 `enrich*` 预处理、SWR 缓存），避免另起炉灶。
5. **TREK 不可照搬的部分**：其核心价值在"多人协作+自托管+全生命周期"，与小程序"单人周末灵感"定位错配；借鉴应聚焦**地图/天气/清单/费用/笔记/统计/导出**这些"让攻略变好用"的通用能力。

---

## 七、结论

TREK 是一份优秀的"旅行工具功能词典"，但**不能直接套用其架构**。对「周末城市游」小程序而言，最有价值的借鉴是：

> **把一份"只读的攻略"，升级成"可规划、可记录、可算账、可沉淀"的轻量出行助手。**

优先做 A 级 5 项（清单/折线/备注/预算/足迹），全部纯前端、低风险、1~2 周可交付；其价值成立的前提，是用阶段 2 把 17 座占位城市的数据替换为真实内容。协作/账号/MCP 等重架构暂不应纳入路线图。

---

*报告基于：TREK 仓库 README/目录结构与功能清单；当前小程序 `miniprogram/`（9 页 + utils）、`server/`、`mockData.js`、`categories.js` 及既有 `v3.0_优化分析报告.md`、`weekend-city-trip_分析报告.md` 的实际代码探查。*
