# 周末城市游 (Weekend City Trip)

一键生成城市周末深度攻略的微信小程序。用户选择城市即可获得结构化的周末出游建议。

## Language（语言）

**攻略** (Guide)：
面向用户的最终呈现，包含行程、活动清单、路线、质量评分等格式化周末出游建议。
_Avoid_：报告（在用户语境中）

**报告** (Report)：
上游 AI 工作流的原始调研输出（API 返回的未经加工数据）。内部数据对象，不直接展示给用户。
_Avoid_：攻略（在 AI 工作流语境中）

**行程** (Itinerary)：
A/B/C 三条主题周末安排，每条含 6 个时间节点的完整时间线。如"文艺青年线"。
_Avoid_：路线（当指向时间安排时）

**路线** (Route)：
CityWalk 纯步行地理路径。如"沙面→沿江西路→永庆坊"。
_Avoid_：行程（当指向地理路径时）

**调研方向** (Research Direction)：
11 个信息采集维度：演唱会、球赛、集市、博物馆、5A景区、喜茶门店、美食街、CityWalk、优惠门票、购物中心、地铁路线。
_Avoid_：分类、类别（作为领域术语时）

**类别** (Category)：
UI 中"活动全清单"和地图过滤等场景的分组标签。
_Avoid_：调研方向（在 UI 标签中）

**收藏** (Favorite)：
用户对城市标记感兴趣（长期关注）。
_Avoid_：关注、保存（针对城市时）

**保存** (Save)：
用户主动留存攻略副本。
_Avoid_：收藏、下载（针对攻略时）

**质量检查** (Quality Check)：
5 维评分体系——完整性、准确性、丰富度、可执行性、信源多样性。每维 0-100 分。
_Avoid_：审核、校验

**地图标记点** (Map Marker / Place)：
地图上的 POI，含分类、坐标、地址、价格、备注。
_Avoid_：标注、图钉

**城市** (City)：
系统支持的 20 个中国城市，含代码、名称、省份、拼音、描述、中心坐标、关键词。
_Avoid_：目的地

**工作流** (Workflow)：
10 步标准化 AI 调研管线——时间锁定→用户确认→建任务+目录→写 query→并行搜索→解析响应→整合报告→质量检查→HTML输出→地图面板。
_Avoid_：流程、管线（在指特定 10 步时）
