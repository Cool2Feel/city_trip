# Weekend City Trip 仓库深度分析报告

> 分析对象：[liangdabiao/weekend-city-trip](https://github.com/liangdabiao/weekend-city-trip)
> 分析日期：2026-07-30
> 仓库许可：MIT
> 最后提交：2026-07-13

---

## 一、项目概述

**Weekend City Trip** 是一个 Claude Code / Codex Skill，专门用于**中国城市的周末 / 短期（1 个月内）旅游深度调研**。用户只需一句话（如"调研下周末广州有什么好玩的"），即可在 7-15 分钟内生成一份覆盖吃喝玩乐行的 20-30KB 纯文字 Markdown 攻略，并可选生成 HTML 报告和交互式地图。

### 一句话定位

> **说一句话 → 11-15 次 anysearch API 调用 → 7-15 分钟 → 20-30 KB 纯文字攻略 → 覆盖吃/喝/玩/乐/行的 10 节标准报告 + 三条周末路线。**

### 项目背景

作者观察到当下年轻人的旅游趋势正在变化：户外消费告别长途远行和装备内卷，转向日常低成本微沉浸；菜市场、城市老街成为精神微旅行目的地，CityWalk 演化为主题探索路线。这个 Skill 正是将"周末去哪玩"的攻略难题，变成一句话的事。

---

## 二、技术架构分析

### 2.1 技术栈构成

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **搜索后端** | anysearch WebSearch API (JSON-RPC 2.0) | 中文索引覆盖好，支持 anonymous 模式 |
| **地图后端** | 高德 AMAP（JS API 2.0 + Web 服务 REST） | 坐标预编码，无需 HTTP 服务器 |
| **报告转换** | Python (markdown → markdown2 → 内置兜底) | 三档优先级降级 |
| **运行平台** | Claude Code / Codex CLI | Skill 作为 `.claude/skills/` 插件 |
| **地点抽取** | Runtime Agent 本体（无外部 LLM 调用） | 由 AI 自身读 Markdown 识别地点 |

### 2.2 架构设计理念

该项目体现了几个关键的架构决策：

**1. 搜索与推理分离**
- 搜索（Step 4-5）→ 交给 anysearch CLI 工具
- 地点抽取/判断/清理（Step 10）→ 由 runtime agent 自身完成，**不调任何外部 LLM API**
- 这种设计降低了 API 成本，同时利用了 agent 本身的推理能力

**2. 坐标服务端预编码**
- 地理编码在生成 HTML 之前完成，写入 JSON
- HTML 打开时只渲染，不调任何外部 API
- 优势：`file://` 协议也能正常打开，可离线分享，不受浏览器 QPS 限制

**3. 三档降级策略**
- Python `markdown` 库 → `markdown2` → 内置极简转换器
- 确保即使没有安装任何依赖，也能生成基本可用的 HTML

### 2.3 项目文件结构

```
weekend-city-trip/
├── SKILL.md                          # Skill 主文件（10 步法 + API 用法）
├── README.md                         # 项目说明
├── LICENSE                           # MIT 许可证
├── references/
│   ├── query_templates.md            # 11 个方向的 query body 模板
│   ├── report_template.md            # 10 节报告标准结构模板
│   ├── anysearch_notes.md            # anysearch 踩坑清单 + 决策树
│   ├── quality_check.md              # 质量检查 + 补查询迭代规则
│   └── map_generation.md             # 地图生成工作流详解
├── templates/
│   └── map_panel.html                # 高德 JS API 2.0 地图模板
└── scripts/
    ├── md_to_html.py                 # Markdown → HTML 转换器
    ├── geocode.py                    # 地理编码脚本
    ├── inject.py                     # 模板注入脚本
    ├── validate_map.py               # 地图质量验证脚本
    ├── known_coords.json             # 预存坐标库
    ├── anysearch_cli.py              # anysearch CLI 工具
    └── build_map.sh                  # 一键管线 orchestrator
```

---

## 三、工作流程深度解析（10 步法）

这是该 Skill 的核心——一套标准化的 10 步调研工作流：

### 流程总览

```
Step 1: 时间锁定 → Step 2: 用户确认 → Step 3: 建任务+目录
→ Step 4: 写 query → Step 5: 并行搜索(5+4+2 三批次)
→ Step 6: 解析响应 → Step 7: 整合报告(初稿)
→ Step 8: 质量检查与迭代(必做，核心差异化)
→ Step 9: HTML 输出(可选) → Step 10: 地图面板(可选)
```

### 各步骤详解

| 步骤 | 名称 | 耗时 | 关键点 |
|------|------|------|--------|
| Step 1 | 时间锁定 | 即时 | 算清"本周末/下周末"对应的具体日期；**关键洞察**：新闻发布时间 ≠ 活动举办时间 |
| Step 2 | 用户确认 | 即时 | 城市和时间明确则跳过，否则用 AskUserQuestion |
| Step 3 | 建任务+目录 | 即时 | 创建 6 个 TodoTask + 工作目录 |
| Step 4 | 写 query | 1-2分钟 | 按 11 个方向写 query body，4 要素法则：地点+主题+时效+品类 |
| Step 5 | 并行搜索 | 30-60秒 | **核心步骤**：11 个 query 拆 5+4+2 三批次并行 |
| Step 6 | 解析响应 | 即时 | JSON-RPC 文本流，按 `--- 标题:` 切分 |
| Step 7 | 整合报告 | 3-5分钟 | 按 10 节标准结构输出初稿 |
| Step 8 | **质量检查** | 2-5分钟 | **核心差异化能力**：5 大维度检查 + 最多 2 轮补查询迭代 |
| Step 9 | HTML 输出 | 30秒 | 条件触发：用户明确要求 HTML |
| Step 10 | 地图面板 | 1-2分钟 | 条件触发：用户明确要求地图，需配置高德 Key |

### Step 5 的批次设计

anysearch 的 `batch_search` 一次支持 1-5 路并行，11 个 query 被精巧地拆为三批：

| 批次 | query 数 | 内容 | 设计理由 |
|------|----------|------|----------|
| 批次 1 | 5 路 | 周末活动主力 + 小红书打卡 + 演唱会集市球赛 + 博物馆5A + 优惠门票 | 信息密度最高的主力查询放第一批 |
| 批次 2 | 4 路 | 喜茶旗舰 + 喜茶主题 + 美食街 + city walk | 偏向静态/长期资源 |
| 批次 3 | 2 路 | 地铁线网 + 地铁出口 | 信息结构化程度最高 |

**总耗时约 30-60 秒**完成全部数据采集。

### Step 8 质量检查体系（核心亮点）

这是整个 Skill 的**差异化核心**——不只是生成报告，还有完整的质量保障闭环：

**5 大质量维度：**
1. **完整性** — 11 个调查方向 + 10 节结构是否齐全
2. **准确性** — 时间/地点/价格是否真实
3. **丰富度** — 每节信息密度是否达标
4. **可执行性** — 周末路线时间是否冲突
5. **信源多样性** — 关键信息是否 ≥ 2 个信源验证

**补查询触发条件（三级优先级）：**

| 级别 | 条件示例 | 行动 |
|------|----------|------|
| 🔴 必补 | 调查方向缺失 / 演唱会<3场 / 跨城市噪声>30% | 不补不能交付 |
| 🟡 建议补 | 喜茶<2家 / 美食街<3条 / city walk 无路线 | 补了显著提升质量 |
| 🟢 可选 | 直达商场<5个 | 时间充裕再补 |

**迭代控制**：最多 2 轮补查询，总 API 调用 ≤ 15 次，防止无限循环。

---

## 四、调研内容体系分析

### 4.1 十一个调查方向

该 Skill 覆盖了城市旅游的全方位信息需求：

| # | 方向 | 时效性 | 核心价值 |
|---|------|--------|----------|
| 1 | 小红书近期活动 | 强时效 | 捕捉年轻人最新打卡趋势 |
| 2 | 演唱会/演出 | 强时效 | 演出排期是周末出行的核心决策因素 |
| 3 | 集市/市集 | 中时效 | 创意市集是年轻人的新兴目的地 |
| 4 | 球赛 | 强时效 | 中超/CBA 主场比赛安排 |
| 5 | 博物馆/美术馆 | 弱时效 | 长期资源 + 当前展览信息 |
| 6 | 优惠门票 | 中时效 | 暑期特惠、考生免费等省钱信息 |
| 7 | 喜茶门店+购物中心 | 弱时效 | 品牌文化打卡 + 商场指引 |
| 8 | 美食街 | 弱时效 | 本地人真正会去的美食街 |
| 9 | city walk 路线 | 弱时效 | 经典步行路线 + 拍照点 |
| 10 | 5A 景区 | 弱时效 | 城市核心景区清单 |
| 11 | 地铁路线 | 弱时效 | 线网总览 + 关键站点出口 |

### 4.2 十节标准报告结构

```
〇、一图速览（表格，10秒拿到核心情报）
一、活动全清单（演唱会/集市/球赛/博物馆/5A）
二、优惠门票
三、喜茶门店热点
四、美食街
五、city walk 路线
六、地铁路线（含5A景区+商场直达）
七、周末组合路线（A/B/C 三条主题路线）
八、时效可靠性说明
九、API 调用统计
十、引用源
```

**设计亮点**：
- 第〇节"一图速览"让用户 10 秒拿到核心情报表
- 第七节"周末组合路线"提供文艺/情侣/家庭三种主题，带时间表
- 第八节"时效可靠性说明"标注"出行前请二次确认"，建立信任
- 第九节"API 调用统计"透明展示每次调用的 query 和返回条数

### 4.3 小红书 query 的关键优化

文档中记录了一个重要的踩坑修复（2026-07）：

- **旧 query**：`小红书 {CITY} 拍照 网红 打卡` → "小红书"被当风格词，召回全是知乎/Instagram 等二手内容
- **新 query**：`site:xiaohongshu.com {CITY} 周末活动 {MONTH}` → 稳定召回真实小红书内容

这个优化体现了作者对搜索 API 行为的深入理解。

---

## 五、地图生成系统分析

### 5.1 四步管线

```
Markdown 报告(.md)
   ↓ Step 1: agent 抽取地点（runtime agent 读 markdown，识别真实地点）
places.json
   ↓ Step 2: geocode.py（高德 REST 批量地理编码）
places.geo.json
   ↓ Step 3: inject.py（模板替换 → 地图 HTML）
{城市}地图.html
   ↓ Step 4: validate_map.py（自动验证地图质量）
验证通过
```

### 5.2 地图交互特性

| 特性 | 说明 |
|------|------|
| 11 类水滴形标记 | 按调查方向分类（演唱会红/球赛紫/集市黄/博物馆蓝...） |
| 分类筛选 pill | 点击切换显示/隐藏类别 |
| 搜索框 | 实时过滤卡片（名称/地址/备注） |
| 双向联动 | 点卡片飞至地图 / 点 marker 高亮卡片 |
| 自动视野适配 | 启动时自动 setFitView 包含所有标记 |
| 响应式布局 | 手机纵向 / 桌面横向 |
| NaN 坐标保护 | 无坐标地点不参与飞至 |
| 离线可用 | 坐标预编码，file:// 协议可打开 |

### 5.3 11 类配色系统

| 类型 | 字母 | 颜色 | Hex | 调查方向 |
|------|------|------|-----|----------|
| 演唱会 | C | 红 | #d32f2f | 演唱会/音乐会 |
| 球赛 | S | 紫 | #7b1fa2 | 体育赛事 |
| 集市 | M | 黄 | #f9a825 | 集市/夜市 |
| 博物馆 | U | 蓝 | #1565c0 | 博物馆/展览 |
| 5A景区 | 5 | 橙 | #e65100 | 5A/4A 景区 |
| 喜茶 | H | 粉 | #ec407a | 喜茶门店 |
| 美食街 | F | 深红 | #ad1457 | 美食街/老字号 |
| City Walk | W | 青 | #00838f | 漫步路线 |
| 购物中心 | L | 紫罗兰 | #4527a0 | 商场 |
| 地铁站 | D | 灰 | #546e7a | 地铁关键站 |
| 优惠门票 | T | 绿 | #2e7d32 | 优惠门票 |

### 5.4 坐标城市错乱的修复记录

文档记录了一个值得注意的 bug 修复历史——"潮州开元寺→泉州"的坐标错乱问题：

| 问题 | 根因 | 修复方案 |
|------|------|----------|
| 部分城市中心在北京 | inject.py CENTERS 字典缺失 | 改为从地点坐标修剪中点自动计算 |
| 地图中心偏离 | min/max 中点受离群值影响 | 改用修剪中点（去除两端10%后取中点） |
| 坐标城市错乱 | known_coords 中"开元寺"存的是泉州坐标 | ① 删除多城市共有地名；② geocode.py 校验 AMAP 响应 city 字段；③ 坐标中国范围检查；④ inject.py 防御性过滤 |

这个修复记录体现了对地理编码精度问题的深入处理。

---

## 六、API 与依赖分析

### 6.1 anysearch API

| 维度 | 现状 |
|------|------|
| 协议 | JSON-RPC 2.0 |
| 字段 | `query` + `max_results`(1-10) |
| 并行 | batch_search 1-5 路 |
| 响应 | `result.content[].text` 纯文本 |
| 分隔符 | `--- 标题: ...\nURL: ...\n摘要: ...` |
| 无 thumbnailUrl | 报告统一纯文字版 |
| 无 freshness | 用 query 关键词隐式表达时效 |
| 无 summary | 只有搜索引擎 snippet |

**关键限制**：
- `max_results` 严格 1-10
- `batch_search` 最多 5 路并行
- anonymous 模式 QPS 极低
- 配额耗尽时 `auto_registered` 字段自动签发新 Key

### 6.2 高德 API

需要**两类 Key**（不能互通）：

| Key 类型 | 用途 | 环境变量 |
|----------|------|----------|
| Web 端 (JS API) | 浏览器加载地图底图 | `AMAP_JS_KEY` + `AMAP_SECURITY` |
| Web 服务 | Python 服务端地理编码 | `AMAP_KEY` |

**免费配额**：Web 服务 3000 次/日，JS API 30 万次/日，个人调研完全够用。

### 6.3 成本估算

**时间成本**：总计 7-15 分钟

| 阶段 | 耗时 |
|------|------|
| 准备 + 写 query | 1-2 分钟 |
| 并行搜索 3 批次 | 30-60 秒 |
| 解析 + 整合 | 3-5 分钟 |
| 质量检查 + 补查询 | 2-5 分钟 |
| HTML 输出（可选） | 30 秒 |
| 地图生成（可选） | 1-2 分钟 |

**API 额度**：
- anysearch：11-15 次 query
- 高德地理编码：30-70 次/城市（日免费配额 3000 次）

---

## 七、优势与亮点

### 7.1 核心优势

1. **标准化 SOP** — 10 步法 + 11 个调查方向 + 10 节报告结构，任何城市一套流程通吃
2. **质量保障闭环** — Step 8 强制检查 + 最多 2 轮迭代优化，不是"生成即交付"
3. **反幻觉机制** — 强制 ≥2 个信源验证 + 显式标注信源 + 出行前二次确认提示
4. **时效性保障** — query 关键词强制带时效词，理解"发布时间 ≠ 举办时间"
5. **失败优雅降级** — markdown 库三级降级 + anysearch 配额耗尽自动续 Key
6. **离线地图** — 坐标服务端预编码，HTML 可离线分享
7. **跨城市标准化** — 城市前缀 + 多区域关键词应对召回波动

### 7.2 工程化亮点

- **query 4 要素法则**：地点+主题+时效+品类，保证召回精准
- **数据复用**：任务 1 的 snippet 通常已含 60-70% 门票信息，任务 2 可直接复用
- **城市关键词速查表**：为广州/深圳/上海/北京/成都预设了购物中心/美食区/地标/5A 景区关键词
- **补查询模板库**：8 个预定义补查询模板，覆盖常见信息缺口
- **自动检查脚本**：Python 脚本扫描报告结构、信息密度、跨城市噪声

### 7.3 文档质量

该项目的文档质量极高：
- README 完整覆盖 16 个章节
- 5 个 reference 文档各司其职，何时读哪个有明确指引
- 踩坑记录详实，包含决策树和状态码表
- 每个文档末尾都有"一句话总结"
- 版本更新记录清晰（如小红书 query 优化、坐标错乱修复）

---

## 八、局限与改进建议

### 8.1 已知局限

| 局限 | 影响 | 建议改进 |
|------|------|----------|
| 仅支持中国大陆城市 | 海外城市召回偏低 | 可考虑接入 Google Search API 作为补充 |
| 无缩略图 | 报告纯文字版 | 可考虑接入图片搜索 API 补充 |
| 地铁出口数据偏旧 | 长尾信息文章可能 2018-2022 | 已通过"二次确认"提示缓解 |
| 不支持 JS 渲染页面 | 复杂 SPA 网页摘要不全 | 可考虑接入 headless browser |
| 单一搜索后端 | 依赖 anysearch 可用性 | 可增加备选搜索后端 |
| 喜茶门店信息更新滞后 | 依赖索引收录速度 | 可考虑接入喜茶官方 API |

### 8.2 改进建议

1. **多搜索后端支持** — 增加 Google/Bing 作为备选，提升海外城市和冷门信息覆盖
2. **增量更新机制** — 缓存已调研城市的数据，避免重复调用
3. **用户偏好学习** — 记录用户常去的城市/活动类型，个性化推荐
4. **实时数据接入** — 对演唱会/球赛等强时效信息，直接接入大麦/猫眼 API
5. **路线优化算法** — 周末组合路线可结合地理距离和时间窗优化
6. **多语言支持** — 翻译为英文，服务海外华人用户
7. **主题扩展** — 增加露营/徒步/骑行等户外主题方向

---

## 九、总结评价

### 9.1 项目成熟度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | ★★★★★ | 11 个方向全覆盖，10 节标准报告 |
| 文档质量 | ★★★★★ | 16 章 README + 5 个 reference，极其详尽 |
| 工程化程度 | ★★★★☆ | 三档降级、质量闭环、一键管线，工程化良好 |
| 可扩展性 | ★★★★☆ | 标准化 SOP 易于扩展新城市，但搜索后端单一 |
| 实用性 | ★★★★★ | 7-15 分钟出报告，直击"周末去哪"痛点 |
| 可维护性 | ★★★★☆ | 文档清晰，但 Python 脚本依赖较多 |

### 9.2 适用人群

- ✅ 经常周末短途旅行的中国城市居民
- ✅ 需要快速了解陌生城市周末活动的出差人士
- ✅ 喜欢结构化攻略而非零散小红书笔记的旅行者
- ✅ Claude Code / Codex 用户

### 9.3 核心价值

这个项目的核心价值在于：**将分散在 N 个平台的信息，通过标准化 SOP + 质量保障闭环，整合为一份可执行的周末行动方案**。它不是简单的搜索聚合，而是一套包含"搜索→整合→检查→迭代→交付"的完整工程化流程，特别是 Step 8 的质量检查体系，是区别于普通 AI 搜索的关键差异化能力。

---

*分析报告完。基于 GitHub 仓库 liangdabiao/weekend-city-trip 的 README.md、SKILL.md 及 5 个 references 文档的完整内容分析。*
