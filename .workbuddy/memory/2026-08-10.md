# 2026-08-10 工作日志

## TREK 对标分析与小程序优化可行性
- 解析了参考项目 liketrek/TREK（自托管协作旅行规划工具，NestJS+React+SQLite+Leaflet/Mapbox GL+MCP/AI）。
- 盘点了当前「周末城市游」小程序：9 页 + utils，api.js 已实现 cloud→api→cache→mock 降级链+SWR，但后端 server 仅为 mockData 壳。
- 关键发现：19~20 座城市仅广州/深圳/成都 3 座有真实编排数据，其余由 generateReport() 用模板+Math.random() 生成占位数据（核心短板）。
- 产出：`TREK对标分析与优化可行性报告.md`。结论：TREK 不可照搬架构；优先借鉴 A 级 5 项（出行清单/路线折线/行程备注打卡/预算聚合/足迹统计），纯前端低风险；前提是用阶段 2 把 17 城假数据替换为真实内容。协作/账号/MCP/3D 地图列为 C 级不建议近期。
- 代码已迭代到 v6/v7（discover v6.0 内容流、profile 含微信登录、search 跨城搜索），比 v3.0 文档更成熟。

## 落地实现：A 级 5 项优化（基于可行性报告）
- 新增 `utils/userData.js`：本地存储层，封装地点标记(想去/已去/备注)、足迹统计、出行清单（模板+勾选+自定义）。纯 wx.storage，无后端依赖。
- A1 出行清单：新增 `pages/packing/`（4 件套），分类模板+勾选进度+自定义增删+本地持久化；已在 app.json 注册、profile 菜单入口。
- A2 路线地图折线：route 页新增可折叠 `<map>`，按时间线节点模糊匹配 places 坐标，绘制 polyline+markers（含 include-points 自适应）。
- A3 个人行程标记/备注：report 页（活动/门票/美食条目标记按钮+「我的标记」管理面板）与 route 页（时间线 POI 标记徽标+备注弹窗）共用 userData；状态循环 无→想去→已去。
- A4 预算估算：route 页按匹配到的票价/餐饮聚合为「门票/餐饮/合计」，区分必花与免费点；无对应数据时优雅降级提示。
- A5 足迹统计：profile 页新增「我的足迹」卡（已探索城市/标记点/已去过/想去），由 userData.getFootprintStats 合并收藏+已保存+标记。
- 版本号 7.1.0→7.2.0；About 文案更新。
- 校验：node --check 5 个 JS 全 OK；app.json/packing.json 合法 JSON。
- 注意：A2/A4 仅在 3 座真实数据城市（广深蓉）效果完整；其余 17 城因 places 为空/随机坐标，地图节点与预算会降级，印证阶段2（真实数据）是前提。

## 阶段2：16 城真实报告批量生成（已完成）
- 用户选择直接做阶段2而非 B 级路线重排。
- 方案：新建 `utils/gen_real_reports.js`（生成器，含 16 城紧凑真实规格）→ 一键展开为 `utils/realCityData.js`（{report,places}，schema 与 generateReport 完全一致，含 aiGenerated 标记）。
- 16 城：上海/北京/杭州/西安/重庆/南京/武汉/厦门/苏州/青岛/昆明/大连/天津/郑州/桂林/长沙。每城 8~13 个真实 POI（含真实坐标、价格、评分、地址）、真实景点/美食街/CityWalk/地铁关键站、3 条路线（节点引用真实 POI 名，使地图折线生效）、真实演出/市集/博物馆活动。
- 接入：`mockData.js` 顶部 require('./realCityData.js')（带 try/catch 守卫，缺失则降级模板）；`generateReport`/`generatePlaces` 优先返回 REAL_REPORTS；`refreshStaticReportWeekend` 兼容（周末动态刷新）。
- 校验：全部 19 城（16 AI + 3 手写广深蓉）均返回真实数据，占位模板清零；路线节点与 POI 匹配 5/6（未匹配者为"市集"类临时活动点，地图优雅降级）；坐标真实非随机。
- UI 标注：`report.js/wxml/wxss` 对 aiGenerated 报告显示「🤖 AI整理·出行前核实」角标，点击弹窗说明需二次核实；版本号 7.2.0→7.3.0。
- 关键提醒：本数据为 AI 基于公开知识整理的近似值，坐标/票价/开放时间出行前请核实。generator 可重跑：`node utils/gen_real_reports.js`。
- 与 A 级联动：阶段2 后，A2 路线地图折线 / A4 预算 / A3 标记在全部 19 城均生效（此前仅广深蓉）。B 级「路线按距离智能重排」仍未做，可作为下一步。

## B 级：路线按距离智能重排（已完成）
- route 页新增「智能重排 / 顺路模式」开关。算法：haversine 直线距离 ×1.35 路网系数估算实际路程；最近邻排序（起点固定第一站=用户所在起点，其余就近排列）；复用 uiHelper.enrichTimelineWithTravel 自动重算每段交通估算。
- 计算原顺序 vs 顺路顺序的路程差，UI 显示「省 X km · Y 分钟」；原顺序已较顺路时显示「当前路线已是较顺路顺序」。
- 联动：切换顺路模式后，时间线展示顺序、地图 polyline 折线顺序、每段交通估算全部同步更新；时间线节点在顺路模式下显示「第N站」+ 灰色「建议时段」参考。
- 仅依赖真实坐标（阶段2 数据），无后端；匹配节点 <2 时按钮禁用并提示。版本号 7.3.0→7.4.0。
- 验证（node 跑真实数据）：多城路线重排后节省 4~18km / 10~44 分钟（如西安A省18.2km、广州B省17km、重庆C省18.2km）；广深蓉/成都部分路线原顺序已顺路，saved≈0，提示逻辑正确。临时验证脚本已删除。

## POI 深化：5 城精度/数量扩充（已完成）
- 用户要求「深化某几座城市的 POI 精度/数量」。选定杭州、西安、重庆、南京、厦门 5 城。
- 在 `gen_real_reports.js` 扩充各城 `pois` 真实坐标与数量：杭州 12→23、西安 12→22、重庆 12→25、南京 12→21、厦门 12→24。新增 POI 含更精确坐标（如杭州苏堤/三潭印月/九溪烟树、西安鼓楼/大明宫遗址/碑林、重庆鹅岭二厂/龙门浩/来福士、南京秦淮河/牛首山/颐和路、厦门演武大桥/胡里山炮台/双子塔等）。
- 重写 5 城各 3 条路线，每条扩到 6~8 节点且**全部引用真实 POI 名**（修掉此前非 POI 节点缺匹配问题：西安C「城市公园」→大明宫国家遗址公园、重庆B「南滨路」、南京B「秦淮河」、杭州C「少年宫」均已变真实 POI）。
- 重跑 `node utils/gen_real_reports.js` 刷新 realCityData.js；临时验证脚本确认：**5 城 POI 数量达标 + 每条路线 matched/timeline.length = 100%（全真实 POI）**。重排节省里程合理（如杭州C省18.5km/44分、西安A省18.4km/44分、南京C省19.1km/46分；个别短路线原序已顺路 saved≈0 提示正确）。
- 版本号 7.4.0→7.5.0（dataVersion，促使用户刷新本地缓存）。临时验证脚本验证后已删除。

## 优化：不同城市数据的实时获取/更新处理（已完成）
- 背景：api.js 已有 SWR + 分级 TTL + 按城市缓存，但存在三类"实时更新"缺口。
- 修复1 版本化缓存键（数据更新自动失效）：api.js 新增 `_dataVersion()`（读 app.js onLaunch 写入 storage 的 `api_data_version`）+ `_vkey()`；对城市级缓存键（report/places/city/center/weather/cities/hot_cities）全部绑定 `_v<版本>`。效果：bump dataVersion 后，全部城市缓存 key 变化→自动失效并重拉，用户无需手动清缓存即可拿到新包数据。app.js onLaunch 写入 `wx.setStorageSync('api_data_version', globalData.dataVersion)`。
- 修复2 按城市清除含变体键：新增 `cache.removeByPrefix` + 模块级 `_removeCityCacheEntries(cityCode)`，改写 `refreshCity`/`clearCityCache` 按前缀清除该城市全部键（report_xxx 含 _w0/_w1/_pX 变体、places/city/center/weather）。修复此前只删裸键 `report_${cityCode}` 导致按城市刷新/失效静默失效的问题。
- 修复3 后台 SWR 刷新回落本地数据：改写 `_backgroundRefresh`，REST/Cloud 源失败时回落到 mockFn（本地打包数据），使 SWR 后台刷新真正基于最新包数据更新（重算周末字符串等），而非因死服务器（localhost:3001 mock 壳）永远不更新。
- UI 呈现：report 页新增 `dataVersion` 字段（api.getDataVersion()），在状态区展示「数据 v7.5.0」小徽标，让用户直观看到"实时更新"到哪个版本；onLoad/下拉刷新/手动刷新均同步该字段。
- 校验：node --check api.js/app.js/report.js 全通过；临时 Node 脚本（mock wx storage）验证：①getReport 写入键含 `_v7.5.0`；②clearCityCache 精准清除 guangzhou 全部变体/版本键、beijing 不受影响；③版本 7.5.0→7.6.0 后 getReport 由 cache 命中变为 api 新鲜拉取（旧缓存失效）。临时脚本已删除。
- 未改 dataVersion 数值（仍 7.5.0），本次交付的是"失效机制"本身；后续任一次打包数据更新只需 bump dataVersion 即可全量生效。

## 修复：home.wxss 本地图片渲染错误（已完成）
- 报错：`pages/home/home.wxss:17:14` 中本地资源 `home-banner.jpg` 无法通过 WXSS 获取（微信限制：WXSS background-image 不能用本地路径，只能用网络图/base64/<image>）。
- 根因：`.hero-bg` 用 `background: linear-gradient(...), url('/assets/images/home-banner.jpg') center/cover` 引用本地图。
- 修复：`home.wxml` 把 `<view class="hero-bg">` 拆成 `<image class="hero-bg-img" src="/assets/images/home-banner.jpg" mode="aspectFill" />`（底层真实照片）+ `<view class="hero-bg-overlay">`（仅渐变，不引用本地 url）；`home.wxss` 相应拆为 `.hero-bg-img`(z-index:0) / `.hero-bg-overlay`(z-index:1，渐变) / `.hero-content`(z-index:2)。文字可读性保持。
- 排查：全 wxss grep `url(` 仅此一处本地图引用；route-art/family/romance.jpg 均经 `<image>` 引用，无问题。编译错误消除。

## 分析：小程序真实数据问题体检（已完成）
- 用户要求"分析小程序真实的数据问题"。写 `analyze_data.js` 复刻 route.js `_matchPlace`(score≥5 命中) 逻辑，逐城体检 19 城（16 AI + 3 手写广深蓉）真实数据。
- 核心发现：**路线时间线节点→POI 坐标命中率 61%~100% 严重不均**（直接决定 A2 地图折线/A4 预算/B 重排是否降级）：
  - 最差：深圳 61%、成都 67%、大连 78%；上海/武汉/郑州 89%；北京/天津/长沙/广州 94%。
  - 100%：已深化的 5 城(杭/西/渝/宁/厦)+ 苏/青/昆/桂。
  - 根因：路线时间线混入两类——真实可落点 POI（公园/街区/场馆，因漏进 places 表而缺坐标）+ 纯行程步骤（午餐/休息/夜市，本就非地点却参与匹配静默失败）。
- 其他体检全通过：坐标 0 越界、重复名 0、评分越界 0。POI 深度不均：均值 16.4（深圳11~广州39）。
- 产出 `data_quality_report.md`：含总表 + 各城未匹配节点 + 分类（"真实地点应补POI"清单 vs "纯行程步骤建议 skipMap"）+ P0~P3 修复建议。analyze_data.js 保留可复跑；中间 JSON 已删。

## 修复：P0 补 POI + P1 非地点标记（已完成，数据问题逐步修复第一步）
- 用户对"确认的问题一步一步修复"。落地 P0（补真实 POI）+ P1（非地点节点 mappable:false）。
- P0 补真实 POI（13 城 16 个）：AI 城在 gen_real_reports.js 对应 pois 追加（上海:安福路周末市集/世纪公园、北京:什刹海、武汉:粮道街/武昌江滩、大连:天津街/东港音乐市集、天津:水上公园、郑州:会展中心、长沙:杜甫江阁）后 `node utils/gen_real_reports.js` 重刷 realCityData.js；手写城（深圳:莲花山公园、成都:正火Live/人民公园鹤鸣茶社、广州:Livehouse）补在 mockData.js PLACES。
- P1 非地点标记：深圳 routes 6 个（创意园午餐/回酒店休息/蛇口夜市晚餐/园区内午餐/华强北晚餐/东门夜市逛街）+ 成都 4 个（奎星楼街午餐×2/九眼桥晚餐+夜景/人民公园喝茶）加 `mappable:false`；route.js `_matchPlace` 开头加 `if(item.mappable===false) return null`，使地图/预算/重排优雅跳过。
- 验证：复跑 analyze_data.js（已同步跳过 mappable:false）→ **全部 19 城有效命中率 100%**（修复前最差深圳61%/成都67%/大连78%）。node --check 4 个 JS 全通过。data_quality_report.md 改为修复后版本（含前后对比+修复清单+剩余 P2/P3）。
- dataVersion 7.5.0→7.6.0（促用户刷新本地缓存拿新 POI）。
- 剩余（非本次强制）：P2 11 城 AI 数据从 12 提至 20+；P3 手写 3 城深度不均（深圳12/成都15 vs 广州40）。

## 优化：确保 API 获取数据的真实有效（三层防线，已完成）
- 背景：api.js 数据层有 SWR+版本化失效，但缺「结构校验」「完整性/溯源」，且 `useRest:true` 指向 `http://localhost:3001`（明文+真机连不上，每次加载先试死服务器浪费约1.5s）。用户选「校验+溯源+关死服务器」全做。
- 防线1 关死死服务器：`CONFIG.useRest: true→false`。前台/后台首拉直接走本地打包数据；接真实后端时改 https 域名再开开关即可。
- 防线2 结构校验（数据有效第一道关）：api.js 新增 `validateReport/validatePlaces/validateWeather` + 分发器 `_validateForType(type,data)`。校验项：report 必含 cityCode、routes 非空(路线有 id/name/timeline)；places 为数组、POI 有名、坐标落中国合理范围(18~53.6N,73~135E)、评分0~5。**关键修正**：报告 routes 在两种结构里位置不同——手写/AI 报告均嵌套在 `sections[type==='routes'].routes`（route.js 即如此读取），少数导出才在顶层 `routes`；故 `_extractRoutes` 统一抽取，避免误判"routes 缺失"把全部真实报告拒掉（已 sweep 19 城 0 false-reject 验证）。`fetchWithFallback` 与 `_backgroundRefresh` 在 REST/Cloud 结果**缓存前**先校验，失败则 quarantine 并回退下一源；mock 末源仍校验告警但不阻断。
- 防线3 溯源（诚实区分实时/缓存）：每次成功拉取挂 `fetchedAt`；report 页 `dataSourceLabel`（实时API/云端同步/本地打包/本地缓存(可能非最新)）随 `result.source+stale` 计算，`dataUpdateTime` 改用 `result.fetchedAt` 让旧缓存显示真实"x分钟前"而非"刚刚"；状态区新增绿色"数据来源"小徽标（缓存非最新显橙色警示）。
- 校验：node --check api.js/report.js 全通过；临时 Node 脚本（mock wx storage）验证三场景：①REST 畸形(缺routes/越界坐标)→被隔离、回退本地打包且数据有效(PASS)；②REST 合法→source=api 命中(PASS)；③校验器单元+19城全量 0 false-reject(PASS)。临时脚本已删除。
- 用户选「C · 构建期烘焙」：不部署 server，key 仅走环境变量不进包，复用 gen_real_reports.js 把权威数据烤进 realCityData.js，运行时零后端零 key。

## 优化：构建期权威源烘焙（选项 C，已落地）
- 用户问"不想部署 server 有代替方式吗"。核实：腾讯有官方 `qqmap-wx-jssdk.js` 可小程序侧直连 apis.map.qq.com（key 绑 appid 仅本小程序可用）；微信云开发云函数（api.js 已预留 useCloud 路径）可托管 key 全量覆盖；构建期烘焙（复用 generator）运行时零后端。用户选后者。
- gen_real_reports.js 改造：新增 env 读取 `TENCENT_MAP_KEY`/`QWEATHER_KEY` + `AUTHORITATIVE` 标志；`geocode(name,region,fallback)` 调腾讯地理编码(v1/geocoder) 出 GCJ-02（与微信 map 组件一致，顺带修正坐标一致性隐患)，`fetchWeather(city,fallback)` 调和风(geoapi 城市Lookup→devapi 3d 取首日)；`buildCity` 改 async、`main` 改 async 带并发限制(limiter(5)/(6))；每个 report 写 `bakedAt`+`authSource`(authoritative/bundled)。**无 key 时全部回落打包坐标，输出与之前一致**。
- report 页如实展示烘焙溯源：状态区新增"权威源烘焙/打包数据 · <bakedAt>"芯片（bakedAt/authSource 在 3 处 setData 注入），与既有 dataVersion/dataSourceLabel 并列。
- 验证：无 key 重跑→16 城生成、node --check 全过、analyze_data.js 19 城命中率仍 100%；带 dummy key 重跑→不崩且优雅回落（沙箱网络阻断 fallback）。dataVersion 7.6.0→7.7.0（促刷新看新溯源）。
- 用法（文档写在 generator 头注释）：`TENCENT_MAP_KEY=xxx QWEATHER_KEY=yyy node utils/gen_real_reports.js` 即可把真实坐标+实时天气烤进包；可挂 GitHub Actions 定时重跑，仍不碰 server。

## 分析：通过接口获取权威数据源（架构方案，未落地）
- 用户问"怎么通过接口获取权威数据源"。结论：复用现有 `server/index.js` 升级为权威源代理，而非把 Key 放前端。
- 数据需求→权威源映射：POI/坐标/距离矩阵/地理编码→腾讯位置服务 WebService（apis.map.qq.com，与微信同生态、坐标 GCJ-02）；天气→和风天气 QWeather（api.qweather.com，免费 1000次/天）。路线 B 级重排当前用 haversine×1.35 近似，可改用腾讯距离矩阵拿真实路网距离。
- 微信硬约束：wx.request 仅 HTTPS + 后台白名单域名；Key 禁放前端→必须经自有 Server 代理（腾讯/和风域名也可加白名单直连但会暴露 Key，不如只加自己服务器域名由后端转发）。
- 落地优势：api.js 已有 useRest 开关 + 版本化缓存 + 校验（刚做），server 端接真源后只需把 restBaseUrl 指向真实 HTTPS 域名并开 useRest，前端几乎不动；envelope 结构一致（{success,data,source,timestamp}）。
- 待注意：① 现有打包坐标坐标系需确认并统一为 GCJ-02（否则上腾讯地图偏移）；② 和风返回天气形状（now.temp/text/icon 为 code）与 server 现 mock（temp/text/icon emoji）不同，server 需做适配层或改 app；③ 真源名称与路线 timeline 文本仍可能不完全一致，_matchPlace 模糊匹配仍需保留；④ 配额限制→服务端缓存+降级到打包数据。

## 配置 TENCENT_MAP_KEY + 配额耗尽处置（已完成）
- 用户提供腾讯地图 WebService key（`OJ4BZ-FSVCB-AG7UD-JMVNO-7EQCF-OZFBI`）。按 C 方案：key 只走 `process.env.TENCENT_MAP_KEY` 运行期注入，**不写进任何源文件/不进包**（CITY_TRIP 非 git 仓库，已避免落盘泄露；GitHub Actions 场景下应存为 repo secret）。
- 首次带 key 重跑：只有 2/258 个 POI 坐标变化，且「南京路步行街」被误解析成**南京**(lat 32.05/lng 118.92) → 产物标 `authoritative` 却几乎全是打包坐标，还混错坐标。根因诊断（test_geo.js 直连腾讯）：`status=121 此key每日调用量已达到上限`——**该 key 当日配额已耗尽**，绝大多数 geocode 被拒回落。
- 处置：① 还原 realCityData.js 为诚实 `bundled`（含正确打包坐标，无错坐标）；② 加固 generator：geocode 增加**跨城误匹配防护**（返回结果 title/address 不含目标城市→回落），彻底避免「南京路→南京」类错误；③ buildCity 按真实命中数逐城标 `authSource`（geoOk>0 或天气 QWeather 命中才标 authoritative，否则 bundled）；④ main() 头部注释也按 `anyAuth` 真实结果写（"密钥已配但请求未成功(配额耗尽)·回落打包坐标"），不再谎称已接入。
- 验证：无 key 重跑→16 城 bundled、header 诚实；带 key 重跑→16 城仍 bundled、header 注明配额耗尽回落（因为 121 全拒），代码端到端可用（node --check 通过）。
- **下一步**：腾讯每日配额通常在北京时间 00:00 重置。配额恢复（或升级 key 配额/把调用 IP 加白名单）后，重跑 `TENCENT_MAP_KEY=xxx node utils/gen_real_reports.js` 即可把 16 城真实 GCJ-02 坐标 + 实时天气烤进包，且跨城误匹配已被防护。临时诊断脚本 test_geo.js/verify_bake.js 已删，备份 .bak 已清。

## 调研：第三方网页数据源 citymap.348349.xyz 可行性
- 用户问小程序能否抓取该网页数据更新、并参考其数据。
- 站点：「中国城市旅游攻略·博查版」静态站（博查 AI 调研生成），覆盖~60城，每城 报告页+地图页。
- 地图页结构：静态 HTML + 高德地图 JS，内联 `TRIP_DATA.places`；每 POI 含 **GCJ-02 真实坐标(lat/lng)+名称+地址+活动备注+分类(section 含活动/路线/地铁站)**。实测上海地图页：53 个 POI、**0 个缺坐标**、坐标系=高德 GCJ-02（与微信 map 组件一致，免转换）。解析方式：括号平衡扫描 `TRIP_DATA = {...}` JS 对象字面量 + eval，可用。
- 报告页：结构化 h2/h3（一图速览/活动清单/优惠门票/美食），无内联坐标（坐标在地图页），标题带具体日期(如"7月4-5日下周末")→**时效性强、现已偏旧（8/10 看已是数周前快照）**。
- 结论：①小程序**运行时直连抓取不可行/不推荐**——wx.request 仅白名单 HTTPS 域名、无 DOM、HTML 解析脆弱、第三方 ToS 风险；②正确架构是**构建期烘焙**（复用 gen_real_reports.js 模式）：Node 离线 fetch+解析 TRIP_DATA→烤进 realCityData.js，运行期零后端，与现有 C 方案一致；③注意：数据有时效性需标注来源日期/过滤过期活动；第三方数据存在版权/授权风险，宜做"参考"而非整库搬运并标明出处。
- 待用户决定：是否构建该构建期抓取生成器（或仅人工参考不入库）。
